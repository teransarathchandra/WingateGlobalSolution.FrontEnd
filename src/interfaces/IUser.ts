export default interface IUser {
    userId: string,
    firstName: string;
    lastName: string;
    email: string;
    contactNumber: string;
    address: string;
    accessToken: string;
    refreshToken: string;
}
